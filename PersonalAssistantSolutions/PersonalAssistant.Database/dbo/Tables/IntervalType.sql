CREATE TABLE [dbo].[IntervalType] (
    [Id]          INT           IDENTITY (1, 1) NOT NULL,
    [InteralType] VARCHAR (100) NOT NULL,
    CONSTRAINT [PK_IntervalType] PRIMARY KEY CLUSTERED ([Id] ASC)
);

